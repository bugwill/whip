# Android release build — 2026-09-22

## Result

The release APK was built successfully with R8 enabled and copied to:

`android/app/build/outputs/apk/release/app-release.apk`

This build is arm64-only (`arm64-v8a`). The repository's `reactNativeArchitectures`
default lists four ABIs, but `react-native-whip-ssh` only supplies
`src/main/jniLibs/arm64-v8a/libwhip_ssh.a`; a four-ABI attempt therefore cannot
link the missing `armeabi-v7a` archive. The retry used the CLI-only override
`-PreactNativeArchitectures=arm64-v8a`; repository architecture settings were
not changed.

Build command (from `android/`):

```text
ANDROID_HOME=/home/ubuntu/Android/Sdk
ANDROID_SDK_ROOT=/home/ubuntu/Android/Sdk
EXPO_NO_DOTENV=1
WHIP_UPLOAD_KEYSTORE_PROPERTIES=<nonexistent temporary path>
./gradlew :app:assembleRelease --no-daemon \
  --init-script <tmpfs redirect-app-build.gradle> \
  -Pwhip.previewSigning=true -PreactNativeArchitectures=arm64-v8a
```

The app build directory and APK archive were redirected to tmpfs during the
build. Preview signing uses the project's debug signing identity as explicitly
selected by `-Pwhip.previewSigning=true`; no upload signing secret was read.

## Artifact evidence

- Package: `io.github.kaminarios.whip`
- Version: `1.7.0` (versionCode `245`)
- Size: `88,822,954` bytes
- SHA-256: `85ad65aeba821c33520b9c526d4d416511ab2cc51a64d83981aafecacf737b15`
- Native ABI entries: `arm64-v8a`
- `aapt2 dump badging`: passed
- `unzip -t`: passed (`No errors detected in compressed data`)
- `zipalign -c -v 4`: passed
- `apksigner verify --verbose`: passed; APK Signature Scheme v2 true, one signer
- R8: enabled (`:app:minifyReleaseWithR8` completed; `whip.skipR8` was not set)
- The build worker did not install or launch the APK. A subsequent ADB
  `install -r` by the device-test worker succeeded; runtime verification is
  tracked in [the device report](2026-09-22-device-test.md).

The previous release APK was preserved before replacement at:

`android/app/build/outputs/apk/release/app-release.pre-ime-owner-fix-20260922.apk`

Backup SHA-256:
`bfe2790f8340232531a2e9503a54a96ec42eba4cbd2e41eb48c68cbcd615049d`

The first local copy was found to be malformed during post-copy validation
(SHA-256 `b5c0c0d03d4c0f86dafac33c99cb3b5245c2552ebc9e8ebabf1ad0b7c2369110`);
it was quarantined in the temporary tmpfs directory. The device still had the
pre-build APK, so a read-only `adb pm path`/`adb pull` recovered the exact
`bfe279…5049d` artifact. Its ZIP CRC, aapt2, apksigner, and zipalign checks
passed before restoring the named backup path. No device install or start was
performed.

The copied release APK was compared byte-for-byte with the validated tmpfs
artifact and its SHA-256 was rechecked after copying. The restored backup was
also compared byte-for-byte with the read-only device pull.

## Build attempts and cleanup

The initial four-ABI attempt failed during native linking because
`armeabi-v7a/libwhip_ssh.a` is not present. An arm64 attempt was then
completed successfully. Temporary tmpfs build roots, Gradle init scripts,
logs, the quarantined malformed copy, and read-only device-pull staging file
were removed after artifact validation; shared Gradle caches and existing
project build state were retained.
